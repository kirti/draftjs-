import { fromJS } from "immutable";

const mentions = fromJS([
  {
    name: "AND Step 1 -> Replacement Workstation?"
  },
  {
    name: "OR Step 3 -> New Company Name",
  },
  {
    name: "AND Step 2 -> IP Address",
  }
  
]);

export default mentions;